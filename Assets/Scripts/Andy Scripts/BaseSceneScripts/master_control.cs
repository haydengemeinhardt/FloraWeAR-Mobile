// This so far serves no purpose other than to have a central location to store this info
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class master_control : MonoBehaviour
{
    public static string plant_used;
    public static string farmwear_used;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
